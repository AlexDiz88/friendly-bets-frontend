import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Exo 2',
    button: {
      fontFamily: 'Exo 2',
    },
  },
});
export default theme;
